# Manual tier pins, applied after the rules in scripts/content/tiers.ts and regenerated
# into content/tiers.yaml by `pnpm content:tiers`.
#
# Use one when a topic's tier is wrong for a reason the rules cannot see: a late-section
# topic readers arrive at directly belongs in tier 1, a first-section topic nobody needs at
# launch belongs in tier 2 or 3. Keys are `{track}/{slug}`, values are 1, 2 or 3.
#
#   overrides:
#     python/decorators: 1
overrides: {}
