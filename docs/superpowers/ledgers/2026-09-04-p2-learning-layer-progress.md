# SDD ledger — plan: docs/superpowers/plans/2026-09-04-p2-learning-layer.md
- Setup: worktree .worktrees/p2-learning-layer on branch p2-learning-layer from main 3644c06 (P1 merged). Execution: Codex per task via scripts/dev/codex-task.sh, Fable reviews; pre-flight conflict scan: tasks share prefs.ts (T1 writes, T3/5/9 read), quiz schema (T2 writes, T3/4/5/9/17 read), practice.ts (T2→T4/6/11), pathmap/paths (T7 only), bilingual/rehype (T12 only), runners (T13 extends P1 protocol), rules.ts (T14, consumed by T14 endpoints + concept card) — no contradictions found; interview schema exists on main only if P0 merged (it has not) → T8 creates it per plan.
- Task 1: dispatched to Codex (BASE 3644c06, brief task-1-codex.md, log codex-p2-t1.log).
- Task 1: complete (4b79639 by Codex; Fable review: exports and tests match the plan, 240 unit tests). Task 2 dispatched to Codex (brief task-2-codex.md, log codex-p2-t2.log).
- Task 2: complete (48b0e07 by Codex; Fable review: catalogue pure + wrapper split, answer-free JSON verified by grep, 250 unit tests). Task 3 dispatched to Codex (brief task-3-codex.md, log codex-p2-t3.log).
- Task 3: complete (0921627 by Codex; Fable review: DOM contract documented, 256 unit tests, islands < 8 kB gz). Task 4 dispatched to Codex (brief task-4-codex.md, log codex-p2-t4.log).
- Task 4: complete (75693cd by Codex; 257 unit, 4 e2e, 10 kata routes, 1,274 links resolve). Fable visual check pending on the built pages. Task 5 dispatched to Codex (brief task-5-codex.md, log codex-p2-t5.log).
- Task 4 visual check (Fable, shots-p2t4): pages match the Kata mockup; deferred to the P2a sweep after Task 11: (a) review/spotbug H1 is the full prompt sentence with a trailing period — add optional quiz item title (≤ 60 chars) to the schema + Task 17 prompt, fall back to prompt without trailing period; (b) the 'review the AI's code' breadcrumb tag should use the acc2 variant; (c) prev/next cards: type tag column looks heavy — use a Tag instead of a filled panel.
- Task 5: complete (8498676 by Codex; 257 unit, 19 e2e incl. 3 checkpoint tests, topic script 31,626 B of 61,440, lhci pass). Task 6 dispatched to Codex (brief task-6-codex.md, log codex-p2-t6.log).
- Task 6: complete (3334a57 by Codex; 6 hub e2e + 16 layout, check:links clean). Fable visual check of the hub screenshot pending/recorded below. Task 7 dispatched to Codex (brief task-7-codex.md, log codex-p2-t7.log).
- Task 6 visual check (Fable, shots-p2-t6): matches the Practice mockup. Sweep notes (P2a sweep after Task 11): (d) when only the kata card has data, the today strip leaves two empty slots — render muted placeholder cards ('appears once you have cards due' / 'once you start a path') or span the kata card; (e) card titles are full prompt sentences — same fix as (a): optional quiz item title.
- Task 7: complete (e7048f7 by Codex; pathmap/paths unit tests + paths e2e; layout checks extended). Fable visual check recorded below. Task 8 dispatched to Codex (brief task-8-codex.md, log codex-p2-t8.log).
- Task 7 visual check (Fable, shots-p2-t7): map matches the Path mockup (columns, diamonds, flow + prerequisite edges, states). Sweep notes: (f) header chips render '23topics' / '5checkpoints' — missing space in the i18n interpolation; (g) once the P0 content merges, most nodes become real links — re-check the map with live topics.
- Codex wrappers pre-written for Tasks 4–17 (task-N-codex.md in this dir); dispatch order stays sequential, one task at a time in this worktree. Task 17 copies the P0 runner files read-only via git show p0-content-pipeline:<path> so the later P0→main merge stays clean.
- Task 8: complete (d96e25c by Codex; 272 unit, 30 e2e incl. interview, FAQPage JSON-LD, 1,404 links resolve). Fable visual check recorded below. Task 9 dispatched to Codex (brief task-9-codex.md, log codex-p2-t9.log).
- Task 8 visual check (Fable, shots-p2-t8): matches the Interview mockup. Sweep note: (h) pluralisation — '1 questions' in section counts; add a plural-aware helper for the en dictionary (zh needs none) and apply it to every '{n} …' count string across the P2 pages.
- Task 9: complete (a6f5829 by Codex; 277 unit, 48 e2e incl. flashcards, answers endpoint, automatic term cards). Fable visual check recorded below. Task 10 dispatched to Codex (brief task-10-codex.md, log codex-p2-t10.log).
- Task 9 visual check (Fable, shots-p2-t9): matches the Flashcards mockup; '1 days' → covered by sweep note (h).
- Task 10: complete (13c8903 by Codex; 285 unit, 38 e2e, print stylesheet, twins + API, 1,502 links resolve; placeholder sheet is reviewed+[calibration] by ruling). Task 11 dispatched to Codex (brief task-11-codex.md, log codex-p2-t11.log).
- Task 10 visual check (Fable, shots-p2-t10): matches the Cheatsheet mockup. Sweep note: (i) 'also for Python' rows show bare labels ('Python') — use the mockup wording ('Python track · N topics', 'Interview bank · N questions', 'Glossary · Python terms').
- Task 11: complete (d4e443d by Codex; nav on, settings rows, continue strip; 285 unit, 159 e2e, lhci 8 URLs pass, 1,785 links). P2a implemented. P2a sweep dispatched to Codex (brief sweep-p2a-codex.md, log codex-p2-sweep.log).
- P2a sweep: complete (4da57cf by Codex; notes a–i applied; 288 unit, 159 e2e, lhci 8 URLs pass, 1,783 links). Whole-branch review of P2a dispatched to Codex (brief review-p2a-codex.md, log codex-p2-review.log).
- P2a sweep visual check (Fable, shots-p2-sweep): placeholders, short kata titles, gutter markers and tags now match the mockups; approved.
- P2a review (Codex) → Not ready: C1 kata answers in HTML/props (ruling: answers fetched on reveal from the .answers.json endpoint); I1 zh placeholders untranslated (ruling: translate now via Codex, translate-ui.md); I2 stripAnswers leaves right/checklist; I3 corrupt progress/flashcards crash checkpoint; I4 path map role=img with links; I5 checkpoint focus loss; I6 lhci forced-reflow-insight red on topic pages; I7 branch deletes two tracked P1 reports. Fix brief ready: fix-p2a-codex.md — NOT dispatched (user asked to pause for a restart). Resume: run it, then a scoped re-review, then merge to main.
- Resumed after restart (2026-09-04 evening). P2a fix round dispatched to Codex (brief fix-p2a-codex.md, log codex-p2-fix.log).
- 2026-09-04 13:02Z: Codex usage limit reached (ChatGPT Codex quota; resets Sep 11 05:45). P2a fix round aborted mid-run (no commits; only the two restored P1 reports staged and a lighthouserc.json edit left). Fallback per memory: Opus subagent runs the fix round from fix-p2a-codex.md.
- P2a fix round: done by Opus fallback (4da57cf..39796eb; 297 unit, 188 e2e, lhci pass, 1,783 links). I6 judged a Lighthouse false positive (unattributed first layout) — forced-reflow kept at warn with the measurement; M2/M4 and sweep note g remain open. Scoped re-review dispatched to Codex (brief rereview-p2a-codex.md, log codex-p2-rereview.log).
- Parallel P2b (user: open as many Codex sessions as needed): branches p2-t12-bilingual, p2-t13-playground, p2-t14-prompt-builder, p2-t15-block-askai, p2-t17-content-runner cut from 39796eb, each with its own worktree, brief copy + parallel-run header (wait for port 4321, append-only shared files, zh values written directly). Task 16 runs last on the merged branch. Controller merges each after review.
- P2a re-review (Codex): 13/14 addressed; I1 NOT addressed (zh residue incl. server-rendered text without JS). Fix round 2 dispatched to Codex (brief fix2-p2a-codex.md, log codex-p2-fix2.log).
- Task 17: complete on p2-t17-content-runner (db0637c by Codex; P0 runner files copied verbatim from 6b2e200-era P0, write.sh + write-brief.ts + 5 prompts, 313 unit tests, five dry runs clean). Merge into p2-learning-layer after fix round 2 lands. Content generation runs must happen in the P0 worktree (staging lives there) after main→P0 merge brings the P2 schemas and the runner.
- Task 14: complete on p2-t14-prompt-builder (88d380f by Codex; 311 unit, 55 e2e; Fable visual check OK). Merged into p2-learning-layer (da4b967): conflicts in remark-callouts.ts (kept the localized-labels version; MARKER export retained) and Cheatsheet.astro (kept Task 14's rules.* keys; dropped the cheatsheets.rules* keys), replaced the P2_RULES_TASK_14 workaround with P2_RULES = true. Gate green (314 unit). Sweep note (j): rules heuristic emits 'Do not assume this is safe: …' prefixes — rewrite pitfalls as clean imperatives.
- Task 12: complete on p2-t12-bilingual (568a74a by Codex; 301 unit, 20 e2e, island 2.6 kB gz; Fable visual check: paired blocks, TOC subtitles, vocab strip match the mockup). Merged into p2-learning-layer (1b426e5); global.css rebuilt as HEAD + T12 append; Topic.astro article attrs merged. Gate green (318 unit, 24 e2e).
- Task 15: complete on p2-t15-block-askai (a4b11c5 by Codex; 305 unit; topic script 52,577 B; Fable visual check: block Ask buttons + try-to-break list OK). Task 13: complete on p2-t13-playground (5ce9d4c by Codex; sql.js vendored, kata tests, share links; Fable visual check OK). Merge of T15 into p2-learning-layer has semantic conflicts in rehype-section-actions.ts (HEAD localized labels vs T15 block buttons) → resolution dispatched to Codex (brief merge-t15-codex.md, log codex-p2-merge-t15.log). T13 merge follows.
- Task 13 merged into p2-learning-layer (4226860): prompts.ts destructuring unioned, P2B_NAV = true replaces the P2B_NAV_TASK_13 workaround, global.css rebuilt as HEAD + T13 append. Gate green (339 unit, 39 e2e, 1,940 links). All P2b task branches merged; Task 16 dispatched to Codex on the merged branch (brief task-16-codex.md, log codex-p2-t16.log).
- Task 16: complete (18ee117 by Codex; 10 Lighthouse URLs all ≥ 0.95 within budgets, no assertion downgraded, axe matrix 14 pages × 2 themes, CI runs check:links + e2e + lhci; docs/dev/learning-layer.md written). P2b whole-branch review dispatched to Codex (brief review-p2b-codex.md, log codex-p2b-review.log).
- P2b review (Codex) → Not ready: I1 Task 17 absent (it had been merged into main by mistake; now merged into p2-learning-layer, 355 unit), I2 Try-to-break links drop code/lang/locale, I3 bilingual pairing unsafe on mismatch, I4 callouts cloned wholesale, I5 zh-en order incomplete, I6 lz payload unbounded, I7 SQL seed unreachable; minors: localization test coverage, stale comments. Fix round dispatched to Codex (brief fix-p2b-codex.md, log codex-p2b-fix.log). Rulings: bi-sig structural signature + refusal; seed fence meta; lz limits 16 kB/100 kB/20 kB.
- P2b fix round: done (c593535 by Codex; 234 e2e, lhci pass). WATCH: topic page script transfer 61,429 B of the 61,440 B budget — 11 bytes of headroom; trim or split the topic islands before P3 (candidates: lazy-load the block Ask-AI menu, defer Bilingual until the toggle is used). Scoped re-review dispatched to Codex (brief rereview-p2b-codex.md, log codex-p2b-rereview.log).
