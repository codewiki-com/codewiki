# Seeded SQL example

```sql seed="users"
CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT NOT NULL);
INSERT INTO users (name) VALUES ('Ada'), ('Grace');
```

```sql run seed="users" title="list-users.sql"
SELECT * FROM users;
```
