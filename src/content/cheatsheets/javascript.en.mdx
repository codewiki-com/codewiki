---
title: JavaScript cheatsheet
description: Node 24 syntax and runtime operations for values, functions, collections, errors, async work, modules, strings, and input boundaries.
track: javascript
terms: [argument, callback, closure, cancellation, destructuring-pattern, dynamic-typing, ecmascript-edition, event-loop]
tags: [reference]
verified: { version: 'Node 24', date: 2026-09-04 }
reviewed: 2026-09-04
status: reviewed
aligned: true
---

<Sheet title="Values">
  <Row code="const value = source">bind a block-scoped name that cannot be reassigned</Row>
  <Row code="let value = source">bind a block-scoped name that can be reassigned</Row>
  <Row code="value ?? fallback">use the fallback only for `null` or `undefined`</Row>
  <Row code="items.at(-1)">read the final item without changing the array</Row>
  <Row code="Object.hasOwn(data, key)">test an own property explicitly</Row>
</Sheet>

<Sheet title="Functions">
  <Row code="const f = (x) => x + 1">return one expression from an arrow function</Row>
  <Row code="function f(x) { return x + 1; }">declare a named function</Row>
  <Row code="f(...values)">spread an iterable into arguments</Row>
  <Row code="f.call(owner, value)">invoke a function with an explicit receiver</Row>
  <Row code="f.bind(owner)">create a function with a bound receiver</Row>
</Sheet>

<Sheet title="Collections">
  <Row code="items.map(project)">build an array by transforming each item</Row>
  <Row code="items.filter(keep)">build an array from matching items</Row>
  <Row code="items.find(match)">return the first match or `undefined`</Row>
  <Row code="items.some(match)">test whether any item matches</Row>
  <Row code="items.every(match)">test whether all items match</Row>
</Sheet>

<Sheet title="Objects">
  <Row code="{ ...base, key: value }">make a shallow copy and override one property</Row>
  <Row code="Object.entries(data)">read enumerable own string-keyed entries</Row>
  <Row code="Object.fromEntries(rows)">build an object from key-value pairs</Row>
  <Row code="delete data.key">remove a configurable own property</Row>
  <Row code="structuredClone(data)">deep-clone supported structured data</Row>
</Sheet>

<Sheet title="Errors">
  <Row code="throw new Error(message)">raise an explicit failure</Row>
  <Row code="try { work(); } catch (error) { handle(error); }">handle a synchronous exception</Row>
  <Row code="promise.catch(handle)">handle a rejected promise</Row>
  <Row code="finally { release(); }">release resources after either outcome</Row>
  <Row code="error instanceof TypeError">test against a known error type</Row>
</Sheet>

<Sheet title="Async">
  <Row code="await task">wait for one promise to settle</Row>
  <Row code="await Promise.all(tasks)">fulfill with every result or reject on the first failure</Row>
  <Row code="await Promise.allSettled(tasks)">collect every fulfillment and rejection</Row>
  <Row code="AbortSignal.timeout(ms)">create a signal that aborts after a timeout</Row>
  <Row code="for await (const item of source)">consume an async iterable in sequence</Row>
</Sheet>

<Sheet title="Modules">
  <Row code="export { value }">export a named binding</Row>
  <Row code="import { value } from './mod.js'">import a named binding</Row>
  <Row code="export default value">export one default value</Row>
  <Row code="import('./mod.js')">load a module dynamically and return a promise</Row>
  <Row code="import.meta.url">read the current module URL</Row>
</Sheet>

<Sheet title="Strings">
  <Row code="`id: ${id}`">interpolate a value into a template literal</Row>
  <Row code="text.includes(part)">test for a substring</Row>
  <Row code="text.replaceAll(a, b)">replace every exact string match</Row>
  <Row code="text.split(separator)">split text into an array</Row>
  <Row code="parts.join(separator)">join values into text</Row>
</Sheet>

<Sheet title="Boundaries">
  <Row code="Number.parseInt(text, 10)">parse a leading base-ten integer</Row>
  <Row code="Number.isFinite(value)">test for a finite number without coercion</Row>
  <Row code="encodeURIComponent(value)">encode one URL component</Row>
  <Row code="JSON.parse(text)">parse JSON text</Row>
  <Row code="JSON.stringify(value)">serialize a JSON-compatible value</Row>
</Sheet>
