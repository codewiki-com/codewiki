---
title: Claude Code essentials
description: Claude Code 2.1.260 commands and prompts for scoped sessions, project context, permissions, review, testing, automation, MCP, skills, and subagents.
track: ai-era
terms: [agent-loop, api-contract, approval-gate, architecture-boundary, characterization-test, context-window, contract-testing, conversation-state, decision-log, deny-by-default]
tags: [reference]
verified: { version: 'Claude Code 2.1.260', date: 2026-09-04 }
reviewed: 2026-09-04
status: reviewed
aligned: true
---

<Sheet title="Launch and diagnose">
  <Row code="claude">start an interactive session in the current directory</Row>
  <Row code="claude 'explain this repository'">start interactively with an initial prompt</Row>
  <Row code="claude -p 'summarize src/auth/'">print one result and exit</Row>
  <Row code="cat test.log | claude -p 'explain the failure'">send standard input with a non-interactive prompt</Row>
  <Row code="claude --version">print the installed Claude Code version</Row>
  <Row code="claude doctor">check installation health and report configuration errors</Row>
</Sheet>

<Sheet title="Scope a task">
  <Row code="Read src/auth/; do not edit yet; map the request flow.">explore the named area without writing</Row>
  <Row code="Plan this change; list files, risks, and verification commands.">require a plan with concrete impact and checks</Row>
  <Row code="Implement issue #123; change only src/auth/ and tests/auth/.">state the deliverable and writable paths</Row>
  <Row code="Preserve the public API; ask before changing the contract.">put an approval gate before an interface change</Row>
  <Row code="Stop if credentials, network access, or a migration is required.">surface capability expansion before acting</Row>
  <Row code="Finish only after tests pass; report commands and exit codes.">define completion with reproducible evidence</Row>
</Sheet>

<Sheet title="Project instructions">
  <Row code="/init">generate or improve starter project instructions</Row>
  <Row code="CLAUDE.md">store shared instructions at the project root</Row>
  <Row code="~/.claude/CLAUDE.md">store personal instructions for every project</Row>
  <Row code="CLAUDE.local.md">store private project instructions and keep them out of Git</Row>
  <Row code=".claude/rules/testing.md">store project rules split by path or topic</Row>
  <Row code="@docs/testing.md">import a file from inside `CLAUDE.md`</Row>
  <Row code="@AGENTS.md">reuse cross-agent instructions from `CLAUDE.md`</Row>
</Sheet>

<Sheet title="Context">
  <Row code="Explain @src/auth/session.ts">attach one file's full contents to the prompt</Row>
  <Row code="Map @src/auth/">attach a directory listing, not every file's contents</Row>
  <Row code="/add-dir ../shared">grant the current session access to another directory</Row>
  <Row code="/context">show what occupies the context window</Row>
  <Row code="/compact keep the failing test output and current plan">summarize the session with an explicit focus</Row>
  <Row code="/clear completed-task">start with empty context and name the previous conversation</Row>
  <Row code="/rewind">restore code or conversation from a checkpoint</Row>
</Sheet>

<Sheet title="Permissions">
  <Row code="/permissions">view and edit allow, ask, and deny rules</Row>
  <Row code="/plan fix the auth bug">enter plan mode and begin the stated task</Row>
  <Row code="claude --permission-mode plan">start a read-only planning session</Row>
  <Row code="claude --permission-mode dontAsk --allowedTools 'Read,Grep,Glob'">approve listed read tools and deny anything else</Row>
  <Row code="claude --disallowedTools 'Read(./.env)' 'Read(./secrets/**)'">deny reads from two sensitive paths</Row>
  <Row code="claude --restricted -p 'inspect this repository'">run one non-interactive check with restricted tools and settings</Row>
</Sheet>

<Sheet title="Sessions and isolation">
  <Row code="/rename auth-refactor">name the current session for later lookup</Row>
  <Row code="claude -c">continue the latest conversation for this directory</Row>
  <Row code="claude -r auth-refactor 'finish the tests'">resume a named session with a new prompt</Row>
  <Row code="claude -r auth-refactor --fork-session">resume as a copy with a new session ID</Row>
  <Row code="/branch alternate-fix">branch the conversation without creating a Git branch</Row>
  <Row code="claude -w auth-refactor">create an isolated Git worktree for a new session</Row>
</Sheet>

<Sheet title="Model and effort">
  <Row code="/model">open the model picker</Row>
  <Row code="/model sonnet">switch models and save the default for new sessions</Row>
  <Row code="/effort high">set the current session's reasoning effort</Row>
  <Row code="/effort status">print the current effort level</Row>
  <Row code="claude --model sonnet --effort high">choose model and effort for this launch only</Row>
</Sheet>

<Sheet title="Review and verify">
  <Row code="/diff">inspect uncommitted and per-turn diffs interactively</Row>
  <Row code="/code-review high">review the current diff for correctness at high effort</Row>
  <Row code="/code-review high 123">review pull request 123 at high effort</Row>
  <Row code="/security-review">check the current branch against the origin default branch for vulnerabilities</Row>
  <Row code="/verify">build and run the application to verify observed behavior</Row>
  <Row code="Compare the diff with the acceptance criteria; cite each gap by file and line.">turn unmet requirements into traceable findings</Row>
</Sheet>

<Sheet title="Specs and tests">
  <Row code="Restate the acceptance criteria as observable behavior before editing.">turn the request into pass-or-fail observations</Row>
  <Row code="Before refactoring, add characterization tests for current behavior.">pin legacy behavior before structural changes</Row>
  <Row code="Reproduce the bug with a failing test before changing production code.">capture evidence that fails before the fix</Row>
  <Row code="Keep existing tests; add boundary, error, and cancellation cases.">cover success paths and relevant failure modes</Row>
  <Row code="Run the narrowest relevant test first, then the full required suite.">get focused feedback before the delivery gate</Row>
  <Row code="Do not weaken assertions or remove tests solely to make the suite pass.">protect the specification from test tampering</Row>
  <Row code="If behavior changes, update the API contract and tests together.">keep implementation, contract, and evidence aligned</Row>
</Sheet>

<Sheet title="Non-interactive automation">
  <Row code="claude -p 'review the current diff' --output-format text">emit plain text and exit</Row>
  <Row code="claude -p 'summarize the diff' --output-format json">emit one JSON result object</Row>
  <Row code="claude -p 'review the diff' --output-format stream-json --verbose">emit streaming JSON events</Row>
  <Row code="claude -p 'return a summary' --json-schema '{&quot;type&quot;:&quot;object&quot;,&quot;properties&quot;:{&quot;summary&quot;:{&quot;type&quot;:&quot;string&quot;}},&quot;required&quot;:[&quot;summary&quot;]}'">validate the final JSON object against a schema</Row>
  <Row code="claude -p 'review the diff' --max-budget-usd 1">cap API-call spending at one US dollar</Row>
  <Row code="claude -p 'review the diff' --max-turns 8">stop with an error after eight unfinished agentic turns</Row>
  <Row code="claude -p 'one-shot check' --no-session-persistence">leave no resumable session</Row>
</Sheet>

<Sheet title="MCP tools">
  <Row code="claude mcp add --transport http sentry https://mcp.sentry.dev/mcp">add a local-scope HTTP server configuration</Row>
  <Row code="claude mcp list">list configured servers and their connection status</Row>
  <Row code="claude mcp get sentry">inspect one server's configuration and status</Row>
  <Row code="claude mcp login sentry">start OAuth authentication for an HTTP server</Row>
  <Row code="/mcp">inspect server status and authentication inside a session</Row>
  <Row code="claude mcp remove sentry">remove the local-scope server configuration</Row>
</Sheet>

<Sheet title="Subagents and skills">
  <Row code=".claude/agents/code-reviewer.md">store a project-scoped subagent definition</Row>
  <Row code="claude --agent code-reviewer">run the whole session as a configured subagent</Row>
  <Row code="/subtask inspect the parser for race conditions">fork a background subagent with the current conversation</Row>
  <Row code="/tasks">view and manage background work in this session</Row>
  <Row code=".claude/skills/fix-issue/SKILL.md">store a reusable project skill</Row>
  <Row code="/skills">list discovered skills and manage their visibility</Row>
  <Row code="/fix-issue 123">invoke a skill and pass `123` as its arguments</Row>
</Sheet>
