---
title: Go 1.27 essentials
description: A printable reference for Go syntax, concurrency, standard-library boundaries, tests, benchmarks, and diagnostic commands.
track: go
terms: [backing-array, backpressure, cancellation, comma-ok-idiom, comparable-type, composition, corpus, deferred-call, error-chain, interface-value, method-set, type-set]
tags: [reference]
verified: { version: 'Go 1.27', date: 2026-09-04 }
reviewed: 2026-09-04
status: reviewed
aligned: true
---

<Sheet title="Declarations and control">
  <Row code="package main">place executable entry code in the main package</Row>
  <Row code="var count int">declare an integer initialized to its zero value</Row>
  <Row code='name := "gopher"'>declare and infer a local variable in the current block</Row>
  <Row code="left, right = right, left">swap values after evaluating both right-hand expressions</Row>
  <Row code="for i := range 10 { use(i) }">iterate with i from zero through nine</Row>
  <Row code="if value, ok := lookup(); ok { use(value) }">branch on a result and status with local scope</Row>
</Sheet>

<Sheet title="Functions and methods">
  <Row code="func add(a, b int) int { return a + b }">declare a function with grouped parameter types</Row>
  <Row code="func load(id string) (Item, error)">return a value together with an error</Row>
  <Row code="func join(parts ...string) string">accept zero or more trailing arguments</Row>
  <Row code="func (c Counter) Value() int">declare a method with a value receiver</Row>
  <Row code="func (c *Counter) Increment()">use a pointer receiver to mutate the referenced value</Row>
  <Row code="func (s Stream[T]) Map[U any](f func(T) U) Stream[U]">declare a method-specific type parameter in Go 1.27</Row>
</Sheet>

<Sheet title="Structs and interfaces">
  <Row code="type User struct { ID int; Name string }">define a struct with exported named fields</Row>
  <Row code='user := User{ID: 7, Name: "Ana"}'>construct a struct with field names</Row>
  <Row code="type Reader interface { Read([]byte) (int, error) }">describe one required behavior</Row>
  <Row code="type ReadWriter interface { io.Reader; io.Writer }">compose interfaces by embedding them</Row>
  <Row code="var _ io.Reader = (*bytes.Buffer)(nil)">check interface satisfaction at compile time</Row>
  <Row code="value, ok := input.(string)">assert a dynamic type without panicking on mismatch</Row>
</Sheet>

<Sheet title="Slices and maps">
  <Row code="items := make([]Item, 0, capacity)">create an empty slice with reserved capacity</Row>
  <Row code="items = append(items, value)">retain the slice returned by append</Row>
  <Row code="clone := slices.Clone(items)">copy elements into an independent backing array</Row>
  <Row code="counts := make(map[string]int)">create a writable map with comparable string keys</Row>
  <Row code="count, ok := counts[key]">distinguish an absent key from a stored zero value</Row>
  <Row code="delete(counts, key)">remove a key; absence is harmless</Row>
  <Row code="keys := slices.Sorted(maps.Keys(counts))">collect map keys in deterministic sorted order</Row>
</Sheet>

<Sheet title="Errors and cleanup">
  <Row code='if err != nil { return fmt.Errorf("load %q: %w", id, err) }'>add context while preserving the error chain</Row>
  <Row code="errors.Is(err, fs.ErrNotExist)">match a target through wrappers and joined errors</Row>
  <Row code="var pathErr *fs.PathError; errors.As(err, &pathErr)">extract the first matching typed error</Row>
  <Row code="errors.Join(errs...)">combine non-nil failures into one traversable error</Row>
  <Row code="defer file.Close()">schedule cleanup when the surrounding function returns</Row>
  <Row code="defer func() { if r := recover(); r != nil { handle(r) } }()">recover only during deferred unwinding in the same goroutine</Row>
</Sheet>

<Sheet title="Goroutines and channels">
  <Row code="go process(job)">start a concurrent call without waiting for its result</Row>
  <Row code="jobs := make(chan Job, 8)">create a channel that buffers up to eight values</Row>
  <Row code="jobs <- job">send, blocking when no receiver or buffer slot is ready</Row>
  <Row code="job, ok := <-jobs">receive and detect when a closed channel is drained</Row>
  <Row code="close(jobs)">signal that no more values will be sent; close from the sender</Row>
  <Row code="select { case result := <-done: use(result); case <-ctx.Done(): return ctx.Err() }">wait for a result or cancellation</Row>
</Sheet>

<Sheet title="Context and synchronization">
  <Row code="ctx, cancel := context.WithTimeout(parent, 2*time.Second); defer cancel()">set a deadline and release its resources on every exit</Row>
  <Row code="ctx, cancel := context.WithCancelCause(parent)">derive a context whose cancel function records an error</Row>
  <Row code="context.Cause(ctx)">read the recorded cancellation cause</Row>
  <Row code="var wg sync.WaitGroup; wg.Go(task); wg.Wait()">start a non-panicking task and wait for it</Row>
  <Row code="mu.Lock(); defer mu.Unlock()">unlock a critical section on every function exit</Row>
  <Row code="once.Do(initialize)">run initialization at most once across goroutines</Row>
  <Row code="value := counter.Add(1)">atomically increment a typed atomic counter</Row>
</Sheet>

<Sheet title="I/O and encoding">
  <Row code="reader := bufio.NewReader(source)">add buffered reads around an io.Reader</Row>
  <Row code="n, err := io.Copy(dst, src)">stream bytes until EOF or the first error</Row>
  <Row code="data, err := io.ReadAll(io.LimitReader(src, limit))">read at most limit bytes into memory</Row>
  <Row code="data, err := json.Marshal(value)">encode a supported Go value as JSON</Row>
  <Row code="err := json.Unmarshal(data, &dst)">decode JSON into a pointer destination</Row>
  <Row code="before, after, found := strings.Cut(text, separator)">split once and report whether the separator existed</Row>
  <Row code="//go:embed templates/*.html">embed matching files in the following variable</Row>
</Sheet>

<Sheet title="HTTP and SQL">
  <Row code='mux.HandleFunc("GET /users/{id}", handler)'>route one HTTP method and path pattern</Row>
  <Row code='id := r.PathValue("id")'>read a named ServeMux path wildcard</Row>
  <Row code="req, err := http.NewRequestWithContext(ctx, method, url, body)">bind cancellation and deadlines to an outbound request</Row>
  <Row code="resp, err := client.Do(req)">send the request; check err before using resp</Row>
  <Row code="defer resp.Body.Close()">release the response body after a successful Do</Row>
  <Row code="err := db.QueryRowContext(ctx, query, args...).Scan(&dst)">execute and scan a query expected to return one row</Row>
  <Row code="tx, err := db.BeginTx(ctx, nil)">start a transaction governed by the context</Row>
</Sheet>

<Sheet title="Modules and build">
  <Row code="go mod init example.com/project">create a module with its import-path prefix</Row>
  <Row code="go get example.com/module@v1.2.3">require or upgrade a dependency at an exact version</Row>
  <Row code="go mod tidy">synchronize go.mod and go.sum with package imports</Row>
  <Row code="go fmt ./...">format every package in the module</Row>
  <Row code="go vet ./...">run standard static analyzers across the module</Row>
  <Row code="go generate ./...">run source generators named by go:generate directives</Row>
  <Row code="go build ./...">compile every package without installing it</Row>
</Sheet>

<Sheet title="Tests and fuzzing">
  <Row code="func TestLoad(t *testing.T)">declare a test discovered by go test</Row>
  <Row code="t.Run(name, testCase)">run a named subtest with a func(*testing.T)</Row>
  <Row code="t.Cleanup(release)">register cleanup after the test and its subtests finish</Row>
  <Row code="go test -race ./...">run all tests with the race detector</Row>
  <Row code="go test -coverprofile=cover.out ./...">write a statement-coverage profile</Row>
  <Row code="func FuzzParse(f *testing.F)">declare a fuzz test with a seed corpus</Row>
  <Row code="go test -fuzz=FuzzParse -fuzztime=30s">fuzz one matching target for thirty seconds</Row>
</Sheet>

<Sheet title="Benchmarks and profiles">
  <Row code="func BenchmarkEncode(b *testing.B) { for b.Loop() { encode(input) } }">measure one operation per iteration with untimed outer setup</Row>
  <Row code="b.Run(name, benchmarkCase)">run a named sub-benchmark</Row>
  <Row code="b.RunParallel(func(pb *testing.PB) { for pb.Next() { work() } })">distribute benchmark iterations across goroutines</Row>
  <Row code="go test -run=^$ -bench=. -benchmem -count=10">skip tests and collect repeated time and allocation results</Row>
  <Row code="benchstat old.txt new.txt">compare benchmark samples statistically</Row>
  <Row code="go test -run=^$ -bench=. -cpuprofile=cpu.prof">capture CPU samples while benchmarks run</Row>
  <Row code="go tool pprof -http=:0 cpu.prof">inspect a profile in a local web interface</Row>
</Sheet>
