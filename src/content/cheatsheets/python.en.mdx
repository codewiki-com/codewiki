---
title: Python cheatsheet
description: The syntax you look up twice a week, on one printed page, with the precise terms to use when asking for more.
track: python
terms: [closure, free-variable, event-loop]
tags: [calibration]
verified: { version: 'Python 3.14', date: 2026-09-04 }
reviewed: 2026-09-04
status: reviewed
aligned: true
---

<Sheet title="Basics">
  <Row code="x: int = 5">annotation is a hint, not a runtime check</Row>
  <Row code="a, *rest = values">star-unpacking keeps the remaining items</Row>
  <Row code="value if ready else fallback">a conditional expression returns one of two values</Row>
</Sheet>

<Sheet title="Functions and scope">
  <Row code="def collect(items=None):">use `None` instead of a mutable default</Row>
  <Row code="nonlocal count">rebind a name in the nearest enclosing function scope</Row>
  <Row code="lambda x: x + offset">a closure reads free variables from its defining scope</Row>
</Sheet>

<Sheet title="Iteration and async">
  <Row code="enumerate(items, start=1)">pair each item with a one-based index</Row>
  <Row code="zip(xs, ys, strict=True)">fail when the input lengths differ</Row>
  <Row code="async with TaskGroup() as group:">keep related tasks in one structured lifetime</Row>
</Sheet>
