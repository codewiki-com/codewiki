en: Pin
zh: 固定指针
aliases: [pinned pointer]
short: { en: 'A pointer wrapper whose safe interface prevents moving an address-sensitive pointee before its destruction completes.', zh: '一种指针包装器，其安全接口防止地址敏感目标在析构完成前被移动。' }
topics: [rust/pin]
