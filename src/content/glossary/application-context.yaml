en: Application context
zh: 应用上下文
aliases: [app context]
short: { en: 'The active Flask scope that provides current_app and g independently of a specific view.', zh: '独立于具体视图，为代码提供 `current_app` 与 `g` 的有效 Flask 作用域。' }
topics: [backend/flask]
