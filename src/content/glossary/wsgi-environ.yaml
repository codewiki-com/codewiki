en: WSGI environ
zh: WSGI environ
aliases: [environ mapping]
short: { en: 'The per-request mapping through which a WSGI server supplies CGI request data, input streams, and execution flags.', zh: 'WSGI 服务器为每次请求提供 CGI 请求数据、输入流和执行标志的映射。' }
topics: [backend/wsgi]
