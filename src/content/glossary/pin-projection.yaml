en: Pin projection
zh: 固定投影
aliases: [pinned projection]
short: { en: 'Deriving access to a field from a pinned parent while preserving the field’s declared pinning status.', zh: '从固定父对象取得字段访问，同时维持该字段声明的固定状态。' }
topics: [rust/pin]
