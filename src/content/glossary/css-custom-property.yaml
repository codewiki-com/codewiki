en: CSS custom property
zh: CSS自定义属性
aliases: [CSS variable]
short: { en: 'A --prefixed CSS property whose value participates in the cascade and can be referenced with var().', zh: '以 -- 开头、值参与层叠，并可通过var() 引用的CSS属性。' }
topics: [frontend/sass]
