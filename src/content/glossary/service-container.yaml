en: Service container
zh: 服务容器
aliases: [IoC container]
short: { en: 'A registry and object factory that maps abstractions to implementations and resolves dependency graphs.', zh: '把抽象映射到实现，并解析依赖对象图的注册表与对象工厂。' }
topics: [backend/php-laravel]
