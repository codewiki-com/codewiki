en: Const assertion
zh: const 断言
aliases: [as const]
short: { en: 'The as const assertion that prevents literal widening and gives object and array literals readonly, tuple-like precision.', zh: '阻止字面量拓宽，并让对象与数组字面量获得只读、元组式精度的 as const 断言。' }
topics: [typescript/literal-types]
