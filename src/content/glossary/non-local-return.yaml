en: Non-local return
zh: 非局部返回
aliases: []
short: { en: 'A return inside an inlined lambda that exits the function enclosing the lambda rather than only the lambda call.', zh: '位于内联lambda中、退出包围该lambda的函数，而非只结束本次lambda调用的返回。' }
topics: []
