en: Request context
zh: 请求上下文
aliases: []
short: { en: 'The active Flask scope that provides request and session proxies for one request.', zh: '为一次请求提供 `request` 与 `session` 代理的有效 Flask 作用域。' }
topics: [backend/flask]
