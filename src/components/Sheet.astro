---
/** One titled group in a printable cheatsheet. Rows arrive through the authored MDX slot. */
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section class="panel cheat">
  <h3>{title}</h3>
  <div class="r2"><slot /></div>
</section>
